Add `handle_turn` operation with the `gen_ai.handle_turn.internal` span and `gen_ai.handle_turn.duration` metric to represent handling of one request to an agentic system, and `gen_ai.main_agent.name` to identify the entity that owns it. Remove the `invoke_workflow` span and `gen_ai.invoke_workflow.duration` metric - workflows are now reported as `invoke_agent` spans with `gen_ai.operation.name` set to `invoke_workflow`.
